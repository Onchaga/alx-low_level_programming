#include "main.h"
/**
 *_isalpha - check if c is in lower or upper case and return 1
 *@c: print this
 *Return: always return 0
 */
int _isalpha(int c)
{
if((c > 'a' && c < 'z') || (c >'A' && C < 'Z'))
{
return (1);
}
else
{
return (0);
}
}

